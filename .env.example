MONGODB_URI=mongodb://localhost:27017/symposium?replicaSet=rs0&directConnection=true
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-nano
# Shared passcode gating the whole site (proxy.ts) — an MVP-demo door,
# not real auth. Pick something you can share out of band with the few people
# who need it.
SITE_PASSCODE=
